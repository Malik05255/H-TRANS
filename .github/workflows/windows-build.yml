name: Windows Installer

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - name: Download official Android Platform Tools
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force "src-tauri\resources" | Out-Null
          Invoke-WebRequest "https://dl.google.com/android/repository/platform-tools-latest-windows.zip" -OutFile "src-tauri\resources\android-platform-tools.zip"

      - name: Download official scrcpy 4.1
        shell: pwsh
        run: |
          $url = "https://github.com/Genymobile/scrcpy/releases/download/v4.1/scrcpy-win64-v4.1.zip"
          $out = "src-tauri\resources\scrcpy-win64-v4.1.zip"
          Invoke-WebRequest $url -OutFile $out
          $sha = (Get-FileHash $out -Algorithm SHA256).Hash.ToLower()
          if ($sha -ne "5b12172b3264b2889f4583ee64752ce832e29bc8b1089dca81093459697165db") {
            throw "scrcpy SHA-256 verification failed: $sha"
          }

      - name: Generate H TRANS installer branding
        shell: pwsh
        run: .\\src-tauri\\windows\\generate-installer-assets.ps1

      - name: Install frontend dependencies
        run: npm install

      - name: Build H TRANS installer
        run: npm run tauri build

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: H-TRANS-Windows-Installer
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
