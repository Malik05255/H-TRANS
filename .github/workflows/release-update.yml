name: Publish Update

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        shell: pwsh
        run: |
          $pkg = Get-Content package.json | ConvertFrom-Json
          "version=$($pkg.version)" >> $env:GITHUB_OUTPUT
          "tag=v$($pkg.version)" >> $env:GITHUB_OUTPUT

      - name: Check existing release
        id: existing
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = "Continue"
          & gh release view "${{ steps.version.outputs.tag }}" *> $null
          if ($LASTEXITCODE -eq 0) {
            "exists=true" >> $env:GITHUB_OUTPUT
          } else {
            "exists=false" >> $env:GITHUB_OUTPUT
          }
          exit 0

      - uses: actions/setup-node@v4
        if: steps.existing.outputs.exists != 'true'
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable
        if: steps.existing.outputs.exists != 'true'

      - name: Download official Android Platform Tools
        if: steps.existing.outputs.exists != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force "src-tauri\resources" | Out-Null
          Invoke-WebRequest "https://dl.google.com/android/repository/platform-tools-latest-windows.zip" -OutFile "src-tauri\resources\android-platform-tools.zip"

      - name: Download official scrcpy 4.1
        if: steps.existing.outputs.exists != 'true'
        shell: pwsh
        run: |
          $url = "https://github.com/Genymobile/scrcpy/releases/download/v4.1/scrcpy-win64-v4.1.zip"
          $out = "src-tauri\resources\scrcpy-win64-v4.1.zip"
          Invoke-WebRequest $url -OutFile $out
          $sha = (Get-FileHash $out -Algorithm SHA256).Hash.ToLower()
          if ($sha -ne "5b12172b3264b2889f4583ee64752ce832e29bc8b1089dca81093459697165db") {
            throw "scrcpy SHA-256 verification failed: $sha"
          }

      - name: Generate H TRANS installer branding
        if: steps.existing.outputs.exists != 'true'
        shell: pwsh
        run: .\\src-tauri\\windows\\generate-installer-assets.ps1

      - name: Install dependencies
        if: steps.existing.outputs.exists != 'true'
        run: npm install

      - name: Build installer
        if: steps.existing.outputs.exists != 'true'
        run: npm run tauri build

      - name: Prepare release files
        if: steps.existing.outputs.exists != 'true'
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          New-Item -ItemType Directory -Force "release" | Out-Null
          $source = Get-ChildItem "src-tauri\target\release\bundle\nsis\*.exe" | Select-Object -First 1
          $installer = "H-TRANS-$version-x64-setup.exe"
          Copy-Item $source.FullName "release\$installer" -Force
          $sha = (Get-FileHash "release\$installer" -Algorithm SHA256).Hash.ToLower()
          $url = "https://github.com/${{ github.repository }}/releases/download/v$version/$installer"
          $json = @{
            version = $version
            url = $url
            sha256 = $sha
          } | ConvertTo-Json
          Set-Content -Path "release\latest.json" -Value $json -Encoding utf8

      - name: Publish GitHub release
        if: steps.existing.outputs.exists != 'true'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ steps.version.outputs.tag }}" "release\H-TRANS-${{ steps.version.outputs.version }}-x64-setup.exe" "release\latest.json" --target "${{ github.sha }}" --title "H TRANS ${{ steps.version.outputs.version }}" --notes "نسخة H TRANS ${{ steps.version.outputs.version }} لنظام Windows."
